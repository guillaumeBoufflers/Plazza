#include	"common.hh"

int	count(const std::string   &input, char c)
{
  int nb;

  nb = 0;
  for (unsigned int i = 0; i < input.size(); ++i)
    {
      if (input[i] == c)
	nb++;
    }
  return (nb);
}
