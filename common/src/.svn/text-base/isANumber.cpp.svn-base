#include	"common.hh"

bool		isNumber(const std::string &s)
{
  for(unsigned int i = 0; i < s.length(); i++)
    if(!((s[i] >= '0' && s[i] <= '9') || s[i] == ' '))
      return (false);
  return (true);
}
